
2.check if the current grub.cfg is up to date with /etc/default/grub
3.check if other operating systems are there by executing os prober and if it takes too long then use grub.cfg to find out
and then if other operating systems are there instruct the user to use 0.0 instead of 0 in timeout
6.improve compatiblity with commented out lines in the /etc/default/grub
improve logging so that logs can be viewed when it crashes